# 台製通｜完整問題清單

**產出日期：** 2026-08-13
**方法：** 先讀完所有可讀資料，再一次列出。不逐題提建議、不邊做邊改規則。
**規則書狀態：** 本清單產出後未再修改 spec 或 skill。任何修改需先經討論。

---

## 讀取範圍

| 已讀 | 未讀 |
|---|---|
| `spec-four-tier-v2.md` (v2.2) | Apps Script 原始碼（需維護者提供） |
| `skill-brand-verification-v3.md` (v3.2) | `test.html`（需 GitHub 授權或另行抓取） |
| `台製通_現況診斷與待決議題_20260731.md` | 測試區「統計」分頁（Chrome 斷線） |
| `台製通_Q5-Q14決議紀錄_20260801.md` | |
| `台製通_Q4決議紀錄_20260803.md` | |
| `台製通_決議對帳表_20260807.md` | |
| `README.md`、`task1`、`task2` prompt | |
| `execution-log-template.csv` | |
| `mit-open-data/`（26 份，244,902 筆） | |
| Sheet：工作表1、回報、待審核、執行紀錄 | |
| **`index.html` 完整原始碼（1,375 行）** | |
| `brands_backup.json` | |
| 前台實際畫面（`?db=test`） | |

---

## 結論先講

**真正會對外造成錯誤的，只有 2 筆資料值。改 2 格即可，不需要動任何規則。**

其餘全部是可記錄、不阻塞的項目。**原本的排程沒有理由停用。**

---

## A. 我（Claude）今天引入的錯誤 — 應先撤銷

| # | 錯誤 | 實情 | 狀態 |
|---|---|---|---|
| A1 | spec v2.2 §10 把「前端改用 `deriveTier`」「撤掉已確認標籤」標為 ❌未做 | **前端早已完整實作**。`index.html` 第 609–617 行有 `deriveTier(eRaw, mRaw)`，四級徽章、篩選、排序全部依 tier；H 欄讀入但不參與判定，畫面上沒有「已確認」標籤 | **待更正** |
| A2 | spec §8／skill §8 把撤下清單來源寫成「回報」分頁 | 正確來源是「執行紀錄」中事件類型＝`刪除品牌` 的列 | 已於 08-13 更正 |
| A3 | 提議「凍結規則書」 | 方向錯誤。應為「先讀完再一次討論」，非停止修改 | 已放棄 |
| A4 | 首次比對資料集時 normalize 方式錯誤，把 `shianey.席艾妮` 的鍵取成 `shianey.`，誤判為「查無」 | 第二次改逐關鍵字掃描才正確 | 已修正 |

**A1 的意義**：08-01 的待辦清單在當時就可能已過期，我未經查證直接沿用，導致 spec v2.2 帶著錯誤的待辦狀態。

---

## B. 會對外造成錯誤 — 建議優先處理（僅 2 筆）

以 2026-08-10 重抓的資料集（244,902 筆）全面複驗 9 筆 E＝是／過期 的結果：

| 列 | 品牌 | E 現值 | 資料集事實 | 前台目前顯示 | 應為 |
|---|---|---|---|---|---|
| **69** | **SANBOO 全MIT輕旅包** | 是 | **查無任何相符紀錄** | 👑 微笑標章（官方背書） | **否** |
| **108** | **可蘭霓 Clany** | 是 | 馥陞企業(84532662) **全數【效期屆滿】** | 👑 微笑標章（官方背書） | **過期** |

**為什麼這兩筆要緊**：前台 `deriveTier` 對 E＝是 直接輸出最高等級徽章「微笑標章」，文案是「通過官方 MIT 微笑標章認證」。這是平台唯一宣稱**官方背書**的等級，而這兩筆的官方紀錄不存在或已失效。

**處理成本**：改 2 格（E69、E108）。不需要改規則。

其餘 7 筆複驗結果正確：

| 品牌 | 資料集事實 |
|---|---|
| Sauber 壁掛空氣清淨機 | 薩柏科技(43856716) 現行有效 5 ✅ |
| shianey.席艾妮 | 仕達企業社(84053022)【有效】✅ |
| FUMI Towel | 台灣日用織品(22257031) 有效 26 ✅ |
| Nature Me | 淨地球(83842181) 有效 0 → E＝過期 ✅ |
| sNug 給足呵護 | 斯傑利企業(16552210) 有效 186 ✅ |
| 牛頭牌 | **三家同名**：喬尼(28646945)屆滿、豐良(22665894)屆滿、**東宜實業鞋廠(09546729)「牛頭牌Newbuffalo」有效**。歸屬靠 F 欄網域 `newbuffalo.com.tw` 確認 ✅ |
| Soletec 超鐵安全鞋 | 吉賦企業(28624864) 全數屆滿；E＝是 依據為品牌提供的驗證書掃描檔（證書號 01800576，2026/07/14 核發）。**8/10 重抓後官方仍未收錄，延遲近一個月** ⚠️ |

---

## C. 事實錯誤，不阻塞

| # | 問題 | 影響 |
|---|---|---|
| C1 | `mit-open-data/data-updated-date.txt` 重抓後未更新，仍寫 `2026-07-22`（實際 8/10 重抓） | skill §1 的「距今超過 40 天」檢查會用錯基準日 |
| C2 | 重抓後總筆數仍為 244,902，與 07-22 版相同 | 官方在此期間未發布新版；Soletec 新證書仍未收錄 |
| C3 | `productlist/` 與 `mit-open-data/` 內容 md5 完全相同，另有 `productlist.zip` | 約 40MB 重複資料 |
| C4 | 「回報」分頁 2 筆回報中，1 列品牌名稱空白，同一則回報被拆成兩列 | 需靠備註文字判斷歸屬；回報流程缺必填驗證 |

---

## D. 結構性缺口 — 記錄，不急

| # | 缺口 | 說明 |
|---|---|---|
| D1 | **E 欄無證據鏈** | M 欄有 O 統編／P 網址／Q 等級／S 日期四欄支撐，E 欄一格都沒有。9 筆 E＝是／過期 中 6 筆 O 欄空白，無法機械複驗。而 E 欄權重高於 M（直接決定最高等級徽章） |
| D2 | **「暫時下架」狀態無欄位** | 前端回報 modal 明寫「平台收到後將**暫時下架**並進行調查」，但資料庫沒有任何欄位可表達這個狀態。目前只能整筆刪除（APD 的做法） |
| D3 | **廠商登錄表單的「申請中」無處可去** | 表單提供三選項：已取得認證／**申請中**／未申請但確為台製。E 欄值域只有 是／否／過期，「申請中」沒有對應值。Soletec 正是這個狀態 |
| D4 | `brands_backup.json` 是空陣列 `[]` | Sheet 讀取失敗時前端顯示零筆品牌。備援機制形同虛設（但至少不會顯示過期資料） |
| D5 | 「待審核」分頁無備註欄 | schema 為 `…I圖片連結｜J提交類型｜K提交時間`，投稿者寫的推薦理由無處可放 |
| D6 | 圖片 | 全庫 344 筆：191 筆無連結、130 筆 Instagram CDN 簽章已於 2026-06-20 全數過期、15 筆 clearbit 自動 logo、3 筆字母頭像。實際產品圖約 1 筆。維護者已決議「先不動」 |
| D7 | 前端只讀 `gid=0` | `CSV_URL = .../export?format=csv&gid=0`，只讀工作表1。O–S 五欄前端未使用 |
| D8 | TKP 統編歸屬未定 | J 欄記「插卡桑企業社(79898140)」，官網為 `tung-hao.com`（同豪科技）。兩家皆為真實高雄登記主體，關係不明。O15 留空 |

---

## E. 前端實作狀態（實際讀碼確認，非推測）

`index.html`（1,375 行）已實作：

- `SPREADSHEET_CONFIG` 含 prod／test 兩組 ID；`?db=test` **同時切換讀與寫**
- `deriveTier(eRaw, mRaw)` 完整四級推導，回傳 `smile／expired／twmade／pending`
- 四級徽章、`filterMit`／`filterVerified` 篩選、微笑標章優先排序
- 測試環境浮水印、測試模式停用 GA
- 回報 modal（5 種回報類型）、消費者推薦／廠商登錄雙表單
- 免責聲明、致謝 `@vk123_zoomin`

**結論：前端不是待辦，是已完成項目。** 今天所有「前台可能沒更新」的擔憂都不成立。

---

## F. 協作方式（待確認）

| 路徑 | 現況 |
|---|---|
| Drive 連接器讀 Sheet | ✅ 可用且穩定 |
| Drive 連接器寫 Sheet 內容 | ❌ 不支援（`update_file` 只改中繼資料） |
| Claude in Chrome 寫 Sheet | ⚠️ 可用但會**靜默失敗**（今日三次整批落空） |
| Apps Script 寫 Sheet | ✅ 最可靠（流水號配號一次成功），但需維護者執行 |
| GitHub 讀寫 repo | ❌ 未授權 |
| 直接抓 GitHub Pages 靜態檔 | ✅ 可讀（本清單即以此讀完 `index.html`） |

---

## G. 建議的下一步（供討論，未執行）

1. 改 2 格：`E69` → 否、`E108` → 過期
2. 更正 spec v2.2 §10 的待辦狀態（前端項目改為已完成）
3. 更新 `data-updated-date.txt`
4. **重啟排程**——B 類只有 2 筆資料值錯誤，與規則無關，原排程沒有理由停用
5. 其餘 C、D 類項目留在本清單，需要時再處理
